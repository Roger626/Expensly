# Render uses this file instead of .gitignore for the deploy artifact upload.
# We must allow dist/ to be included so the compiled app can run.
node_modules
backend/node_modules
frontend/node_modules
concepts/node_modules
