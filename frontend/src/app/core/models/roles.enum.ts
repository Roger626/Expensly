/**
 * Roles de usuario disponibles en la plataforma.
 * Deben coincidir con el enum RolUsuario del backend.
 */
export enum Role {
  SUPERADMIN = 'SUPERADMIN',
  CONTADOR   = 'CONTADOR',
  EMPLEADO   = 'EMPLEADO',
}
